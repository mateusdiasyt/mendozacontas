# MendozaContas - Variáveis de ambiente
# Copie para .env e preencha. Nunca commite .env.

# Banco (Neon.tech)
# URL com pooler para a aplicação (recomendado para Vercel):
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# URL direta (sem -pooler) para migrações. Opcional se usar só DATABASE_URL:
# DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# JWT (obrigatório em produção)
JWT_SECRET="gere-um-secret-forte-aqui"
